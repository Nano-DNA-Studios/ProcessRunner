using NUnit.Framework;
using NanoDNA.ProcessRunner.Results;

namespace NanoDNA.ProcessRunner.Tests
{
    internal class ResultTests
    {
        /*private class DummyContent
        {
            public string Text { get; set; }
            public DummyContent(string text) => Text = text;
        }

        [Test]
        public void Constructor_SetsContentAndMessage()
        {
            var content = new DummyContent("test");
            var result = new Result<DummyContent>(content, "success");

            Assert.That(result.Content, Is.EqualTo(content));
            Assert.That(result.Message, Is.EqualTo("success"));
        }

        [Test]
        public void Constructor_WithNullMessage()
        {
            var content = new DummyContent("null-check");
            var result = new Result<DummyContent>(content);

            Assert.That(result.Content.Text, Is.EqualTo("null-check"));
            Assert.That(result.Message, Is.Null);
        }*/
    }
}
