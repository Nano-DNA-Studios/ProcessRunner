<Project Sdk="Microsoft.NET.Sdk">

	<!-- Project Reference Conditionals -->
	<PropertyGroup>
		<AutomationResultsProjectExists Condition="Exists('$(MSBuildThisFileDirectory)..\..\AutomationResults\AutomationResults\AutomationResults.csproj')">true</AutomationResultsProjectExists>
		<AutomationResultsProjectExists Condition="'$(AutomationResultsProjectExists)' == ''">false</AutomationResultsProjectExists>
		<UseProjectReference Condition="'$(Configuration)' == 'Debug' and '$(AutomationResultsProjectExists)' == 'true'">true</UseProjectReference>
		<UseProjectReference Condition="'$(Configuration)' == 'Release'">false</UseProjectReference>
	</PropertyGroup>

	<!-- Building / Publishing Metadata -->
	<PropertyGroup>
		<RootNamespace>NanoDNA.ProcessRunner</RootNamespace>
		<ProductName>$(RootNamespace)</ProductName>
		<PackageId>$(RootNamespace)</PackageId>
		<AssemblyName>$(RootNamespace)</AssemblyName>
		<OutputType>Library</OutputType>
		<IsPackable>true</IsPackable>
		<DebugType>embedded</DebugType>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<!-- Add Individual Platforms in NuGet Package -->
	<ItemGroup Condition="'$(Configuration)' == 'Release'">
		<None Include="..\README.md" Pack="true" PackagePath="" />
	</ItemGroup>

	<!-- Development Project References -->
	<ItemGroup Condition="'$(UseProjectReference)' == 'true'">
		<ProjectReference Include="..\..\AutomationResults\AutomationResults\AutomationResults.csproj" />
	</ItemGroup>

	<!-- Include Items to Edit -->
	<ItemGroup>
		<!-- Project Documents -->
		<None Include="..\README.md" Link="Docs\README.md" />
		<None Include="..\Directory.Build.props" Link="Docs\Directory.Build.props" />
		<None Include="..\Directory.Packages.props" Link="Docs\Directory.Packages.props" />
		<None Include="..\LICENSE" Link="Docs\LICENSE" />

		<!-- Workflow Scripts -->
		<None Include="..\.github\workflows\build.yml" Link="Workflows\build.yml" />
		<None Include="..\.github\workflows\publish.yml" Link="Workflows\publish.yml" />
		<None Include="..\.github\workflows\test.yml" Link="Workflows\test.yml" />
	</ItemGroup>

	<!-- NuGet Package References -->
	<ItemGroup>
		<PackageReference Include="NanoDNA.AutomationResults" Condition="'$(UseProjectReference)' == 'false'" />
		<PackageReference Include="NLog" />
	</ItemGroup>
	<ItemGroup>
		<None Update="NLog.config">
			<CopyToOutputDirectory>Always</CopyToOutputDirectory>
		</None>
	</ItemGroup>

</Project>
